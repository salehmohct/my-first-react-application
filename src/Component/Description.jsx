import React from "react";
import { CustomizedDesc } from "../Global/Style";
const Description = (props) => (
  <CustomizedDesc className="Description">{props.Desc}</CustomizedDesc>
);

export default Description;
